//package com.rb.authentication.controller;
//
//import org.springframework.http.ResponseEntity;
//import org.springframework.security.core.context.SecurityContextHolder;
//import org.springframework.security.core.userdetails.User;
//import org.springframework.stereotype.Controller;
//import org.springframework.web.bind.annotation.RequestMapping;
//
//@Controller
//public class RestController {
//
////    @RequestMapping("/api/users/me")
////    public ResponseEntity<UserProfile> profile()
////    {
////        //Build some dummy data to return for testing
////        User customer = (User) SecurityContextHolder.getContext().getAuthentication().getPrincipal();
////        String email = customer.getUsername() + "@howtodoinjava.com";
////
////        UserProfile profile = new UserProfile();
////        profile.setName(customer.getUsername());
////        profile.setEmail(email);
////
////        return ResponseEntity.ok(profile);
////    }
//}
